/* global
functions, getUnix,
objects, moment, constants, AVAILABLE_PERIODS, TRADING_CONSTANTS,
classes, LightweightCharts, TradingDemo
*/

class TradingDemoOnline extends TradingDemo {
  // checkTrades(instrumentDoc, candleData) {
  //   const targetTrades = this.trades.
  // }
}
